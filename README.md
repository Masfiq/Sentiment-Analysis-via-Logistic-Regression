# Sentiment-Analysis-via-Logistic-Regression

Sentiment analysis classifier built from scratch in Python using logistic regression with mini-batch gradient descent. Trained and evaluated on movie review datasets to classify text as positive or negative, with precision, recall, F1, and accuracy reporting.
